apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  # - ingress-http.yaml
  # - ingress-grpc.yaml

# 命名空间
namespace: tiktok

vars:
  - name:
    objref:
      kind:
      name:

# 标签
#commonLabels:
#  app: bingo
#commonAnnotations:
#  oncallPager: 800-555-1212
