apiVersion: apps/v1
kind: Deployment
metadata:
  name: simple-bank-api
  labels:
    app: simple-bank-api
spec:
  template:
    spec:
      containers:
        - name: simple-bank-api
          resources:
            limits:
              memory: "200Mi"
              cpu: "200m"

